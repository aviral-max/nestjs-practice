import { Injectable } from '@nestjs/common';

// Service = contains all the business logic.
// This is where you write functions like login(), signup(), validateUser(), etc.
@Injectable()
export class AuthService {
  // Example later:
  // login() { ... }
}
